<div id="all_content3">
    <div id="col_content">
        <div id="breadcrumbs">
            <a href="<%=application_url%>">rede</a>
            > fórum > postagens > destaques
        </div>
		
		<%- @section = 'posts' %>
		<%-box :class => "alt first_alt" do %>
			<label for='title'>
				<%= :featured_writers.l %>
			</label>
			<%- @featured_writers.each do |user| %>
				<%= render :partial => "users/sidebar_user", :locals => {:user => user} %>
			<% end %>
		<% end %>
		<%-box :class => "hfeed" do %>
			<label for='title'>
				<%= :featured_posts.l %>
			</label>
			<%= paginating_links @posts if @posts.page_count > 1 %>
			<%= render :partial => 'posts/post', :collection => @posts %>
		<% end %>
		
		
	</div>
</div>	
