<% partner = Partner.find_by_email("cns@redu.com.br") %>
<%= form_for :partner_contact, :url => contact_partner_path(partner),
  :html => { :id => "partner-contact-form", :method => :post, :class => "form-horizontal", :remote => "true" } do |f| %>
  <%= concave_error_messages_for(:partner_contact) %>
  <%= render :partial => 'partners/partner_contacts/form', :locals => {:f => f, :environment => environment, :migration => migration ||= false, :billable_url => billable_url ||= nil } %>
<% end %>
