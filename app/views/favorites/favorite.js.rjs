if @favorite.new_record?
  #page.select('#flash_messages').invoke('update', flash[:error])
  #page << "new Effect.Highlight($$('flash_messages')[0]);"
  page.reload_flash
else
  page.select('#favorito').invoke('update', "<span class=\"favorites_off\" >Favorito</span>")
  page.reload_flash
  #page.select('#flash_messages').invoke('update', flash[:notice])
  #page << "new Effect.Highlight($$('flash_messages')[0]);"
end

page.select('#favorite_spinner').invoke('hide')
          

