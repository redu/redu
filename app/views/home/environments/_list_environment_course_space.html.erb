<%#
  Renderiza a lista de ambientes-cursos-disciplinas.

  Variáveis:
    enviroments: os ambientes.
%>
<% if environments.empty? %>
  <%# TODO: O que fazer quando não há nenhum ambiente? %>
<% else %>
  <ul class="list-environment-course-space">
    <%= render :partial => "home/environments/list_environment",
               :collection => environments,
               :as => :environment %>
  </ul>
<% end %>
