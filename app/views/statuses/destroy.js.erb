$("#status-<%= @status.id %>").slideUp(function(){
  $countAtt = $(this).closest(".responses");
  if ($(this).siblings("li").length == 2) {
    $(this).parents(".responses").hide();
  };
  $(this).remove();
  $countAtt.countComments().groupResponses();
});
