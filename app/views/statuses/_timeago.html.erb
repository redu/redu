<%#
  Detalhe da data e horário.

  Variáveis:
    timestamp: detalhe da data e horário.
%>

<%= content_tag :time, l(timestamp, :format => :time_ago),
  :class => "date legend", :datetime => timestamp.to_time.iso8601 %>