<%#
  Link com o nome completo do usuário para o seu perfil.

  Variáveis:
    user: o usuário.
    classes: classes extras.
%>

<% classes = nil unless local_assigns.has_key? :classes %>

<%= link_to user.display_name, user_path(user), class: "link-name #{classes}" %>