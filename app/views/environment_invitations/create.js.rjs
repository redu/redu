if @invitation.valid?
  page.replace_html "environment_invitation_errors",
    "<div class='box_notice'>Convite enviado com sucesso!</div>"
  page["environment_invitation"].reset
else
  page.replace_html "environment_invitation_errors",
    error_messages_for(:invitation)
end
