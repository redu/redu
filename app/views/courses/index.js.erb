$('#courses_list').append(" <%= escape_javascript (
    render :partial => 'courses/item_detailed', :collection => @courses
  ) %>");

$('.endless').replaceWith(
   "<%= escape_javascript (will_paginate @courses,
              :renderer => EndlessRenderer,
              :params => { :search => params[:search],
                           :audiences_ids => params[:audiences_ids] }) %>"
    );
