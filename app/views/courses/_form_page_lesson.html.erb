<% f.fields_for :lesson do | build |%>


Conteúdo:
<br>
<%= build.text_area :body, :size => "80x25", :class => "mceEditor" %>
<br>
<br>
<%= build.hidden_field(:position, :value => "0") %>
<%= build.hidden_field(:_destroy, :value => false, :id => "destroy_" + ((build.object.id) ? build.object.id.to_s : '0'))  %> 
<br>
<br>

<% end %>