<div class="grid_16">
<div id="breadcrumbs">
		<% if @school %>
	<a href="<%=application_url%>">início</a> > <a href="#">redes</a> > <%= link_to @school.name, @school %> > nova aula
	<% else %>
	<a href="<%=application_url%>">início</a> > <a href="#">ensinar</a> > nova aula
	<%end %>
</div>

<%= render(:partial => "schools/school_info", :locals=>{:show => false}) if @school%>	

<!--
<h3 class="call">Nova Aula - Publicação</h3>
-->
<div id="title_video">Nova Aula - Publicação</div>

<div class="grid_9 push_3">
<table class="booking_steps" cellpadding="0" cellspacing="0">
							<tbody><tr>
								<td><span class="book_step"><span><em>1</em>Informações Gerais</span></span></td>
								<td class="spacer">&nbsp;</td>
								<td><span class="book_step"><span><em>2</em>Editor de Aula</span></span></td>
								<td class="spacer">&nbsp;</td>
								<td><span class="book_step current"><span><em>3</em>Publicação</span></span></td>
							</tr>
						</tbody></table>
</div>


<div class="grid_8">

<% form_for :course, @course, :url => { :action => "create" }, :html => {:id => "form_step_3",  :multipart => true } do |f| %>
<%= f.error_messages %>
<%= hidden_field_tag("step", "3", options = {})%>
	<%= hidden_field_tag("post_to", @school.id) if @school %>



	<% if @course.courseable_type = 'InteractiveClass' %>
	    <% field_set_tag 'Imagem' do %>
	        <%= f.label 'Defina uma imagem para esta aula' %>
	        <%= f.file_field :avatar %>
	    <% end %>
    <%end %>



	<% field_set_tag 'Visibilidade' do %>
	<%= f.label "Pública <span class='snippet'>(Visível em todo o Redu)</span>" %>
	<%# :onchange => "jQuery('input[name=post_to]').attr('checked', false)" %>
		<%= f.check_box :public %>
		
		<% @schools = current_user.schools %>
		<% if @schools and @schools.size > 0 %>
		<%= f.label "Postar em:" %>
			<div style="overflow-y:scroll; max-height: 200px;">
			<% @schools.each do | school | %>
				<div><%= radio_button_tag :post_to, school.id, false %> <%= school.name %></div>
			<% end %>
			</div>
		<% end %>
		<%end%>
	
	
    <% if AppConfig.activate_payment %>
	    <% field_set_tag 'Preço' do %>
	        <%= f.label :price %>
	        <%= f.text_field :price %>
	    <% end %>
    <%end %>


	<% field_set_tag 'Compartilhe!' do %>
		<%= render :partial => 'shared/share', :locals => {:resource => @course}%>
		<%end%>

</div>

<div class="grid_6 push_1">
											<div class="y_addinfo">
                                <div class="addinfocontent">
                                    <h1>
                                        Termos e Condições do Redu</h1>
                                    <p>
                                       Por favor não publique conteúdo que esteja protegido por direitos autoriais, conteúdo obceno ou que viole
																			 os <%= link_to "termos e condições", {:controller => "base", :action => "tos"}, :target => "_blank"%> do Redu.
                                    		
																			Ao clicar no botão abaixo você estará concordando com esses termos.</p>
																			
																			<p><span style="font-style: italic;">Uma vez publicado, as opções de publicação não poderão mais serem editadas.</span></p>
																			
																		<p class="large_btn"><a href="#" id="adm_showpay" style="color: #fff;" onclick="jQuery('#form_step_3').submit();">Publicar</a></p>
                                </div>
                            </div>
</div>

<div class="grid_16">
<%= link_to '< Anterior', new_course_path(:step => '2'), :class => "button", :disabled => true %> 
<%# f.submit 'Salvar' %> ou
<%= link_to 'Cancelar', cancel_courses_path, :confirm => "Tem certeza que deseja sair do editor de aulas? Todas as alterações não salvas serão perdidas." %>
</div>

<% end %>

</div>
</div>



