<%#TODO incluir cabaçalho de environment %>
<% form_for @course,
  :url => environment_courses_path(@environment) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'courses/form', :locals => { :f => f } %>
  <%= f.submit "Criar!" %>
<% end %>
