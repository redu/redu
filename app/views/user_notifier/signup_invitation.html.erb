Olá!

<%= @user.login %> (<%= @user.email %>) convidou você para entrar no <%= AppConfig.community_name %>. <%= AppConfig.community_description %>

Ele te enviou a seguinte mensagem:
<%= @message %>

Visite o link abaixo para se cadastrar e aproveitar o que o Redu oferece:

<%= @url %>

Obrigado,
Equipe <%= AppConfig.community_name %>