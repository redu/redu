Olá!

<%= @user.login %> (<%= @user.email %>) convidou você para entrar no <%= Redu::Application.config.name %>. <%= Redu::Application.config.description %>

Ele te enviou a seguinte mensagem:
<%= @message %>

Visite o link abaixo para se cadastrar e aproveitar o que o Redu oferece:

<%= @url %>

Obrigado,
Equipe <%= Redu::Application.config.name %>
