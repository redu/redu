page.insert_html :bottom, "bulletins", :partial => 'bulletins/list',
  :collection => @bulletins, :as => :bulletin, :locals => {:is_index => true}
page.replace_html ".endless",
  (will_paginate @bulletins, :renderer => EndlessRenderer)
