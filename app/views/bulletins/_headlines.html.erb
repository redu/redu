<ul class="bulletins-headlines">
  <% headlines.each do |item| %>
	<li>
          <%= link_to item.title, polymorphic_path([item.bulletinable, item]), :class => 'title'  %>
          <span class="date quiet"><%= time_ago_in_words(item.created_at) %></span>
        </li>
  <% end %>
</ul>
