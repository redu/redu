page.insert_html :bottom, 'users_list', :partial => "users/new/item",
  :collection => @users, :locals => { :entity => @space }

page.replace_html ".endless",
  (will_paginate @users, :renderer => EndlessRenderer)
