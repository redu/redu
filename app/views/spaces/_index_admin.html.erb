<% if @spaces && @spaces.size > 0 %>
  <table id="spaces_list" class="common">
    <thead>
      <th>Nome</th><th>Editar</th><th>Deletar</th>
    </thead>
    <%= render :partial => 'spaces/item_admin', :collection => @spaces %>
  </table>
  <%= will_paginate @spaces, :renderer => EndlessRenderer %>
  <%= link_to 'Criar Disciplina', new_space_path(:course_id => @course.id), :class => 'button' %>
<% else %>
  <div class="box_notice">
    Não existe nenhuma disciplina.
    <!--TODO Colocar ajax para o form aparecer e sumir -->
    <%= link_to "Criar uma disciplina", new_space_path(:course_id => @course.id) %>
  </div>
<% end %>
