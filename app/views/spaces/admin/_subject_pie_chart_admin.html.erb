<%= content_tag(:div, :class => "subject-participation-pie",
               :id => "subject-participation-pie-#{subject.id}") do %>
  <%= lazy_load(:chart, :jammit => true) do %>
    try{
      var pieSbjPar = subjectParticipationPie();
      pieSbjPar.load("<%= Redu::Application.config.vis[:activities] %>",
                     "subject-participation-pie-<%= subject.id %>",
                     "<%= subject.id %>");
    }catch(e){
    }
  <% end %>
<% end %>
