<% if action_name == 'site_index' %>
	<%= stylesheet_link_tag 'jcarousel' %>
	<%= javascript_include_tag 'jquery.jcarousel.min.js'%>
	<script type="text/javascript"> 
	 
	jQuery(document).ready(function() {
	    jQuery('#carousel').jcarousel();
	});
	 
	</script>
<% end %>

<% if controller_name == 'users' and action_name == 'show'%>
<%=  javascript_include_tag "jquery.editinplace.js"%>
<% end %>

<% if controller_name == 'admin' or controller_name == 'beta_keys' %>
<%= javascript_include_tag "tablecloth.js" %>
<%= stylesheet_link_tag "tablecloth.css" %>
<% end %>


  <!-- upload | uploadify -->
<% if controller_name == 'coursesADSLLD' and action_name == 'new'%>
<%=  stylesheet_link_tag 'uploadify' %>

<%- session_key_name = ActionController::Base.session_options[:key] -%>
<%=  javascript_include_tag "swfobject", "uploadify/uploadify210.js" %>
<% url = courses_path %>
<script type="text/javascript">
  $(document).ready(function() {
    $('#seminar_media').click(function(event){
     event.preventDefault();
     });
    $('#seminar_media').uploadify({
      'uploader' : '/javascripts/uploadify/uploadify.swf',
      'script' : '/courses',
      'multi' : false,
      'cancelImg' : '/images/cancel.png',
     'scriptData': {
     '_session_id' :  '<%= u cookies["_session_id"] %>',
     'authenticity_token' : '<%= u cookies["auth_token"] %>'
     }
    });
        
    $('#seminar_submit').click(function(event){
                       if ( $("input:checked").val() == 'upload') {
                               event.preventDefault();
                                 $('#seminar_media').uploadifyUpload();
                       }                
               });
});
</script>


<%end%>
<!-- end upload -->
		




<% if controller_name == 'schools'  %>
	<% if action_name == 'show'%>
	<%= stylesheet_link_tag 'files' %>
	<%= ajaxful_rating_style %>
	<% end %>
  
 <% end %>
 
 <%  if @school and not @school.new_record? %>
    <%= stylesheet_link_tag 'themes/' + @school.theme %>
  <% end %>
 
 <%= include_tiny_mce_if_needed %>
 
 <% if controller_name == 'courses' || controller_name == 'exams' || controller_name == 'bulletins' || controller_name == 'favorites' %>
  <%= ajaxful_rating_style %>
  <% end %>
	
	<% if controller_name == 'courses' && action_name == 'show'%>
  <%= javascript_include_tag 'swfobject'%>
	<% end %>
	
	<% if controller_name == 'events' %>	
		<%= javascript_include_tag 'prototype'  %>
		<%= calendar_date_select_includes :locale => 'pt' %>
	<% end %>
	
	<% if controller_name == 'schools' || controller_name == 'events' || controller_name == 'bulletins' %>
		<%= stylesheet_link_tag 'calendar/red/style' %>
	<% end %>
	
	<script type="text/javascript">
var uservoiceOptions = {
  /* required */
  key: 'redusugestoes',
  host: 'redusugestoes.uservoice.com', 
  forum: '58955',
  showTab: true,  
  /* optional */
  alignment: 'right',
  background_color:'#06C', 
  text_color: 'white',
  hover_color: '#06C',
  lang: 'pt_BR'
};

function _loadUserVoice() {
  var s = document.createElement('script');
  s.setAttribute('type', 'text/javascript');
  s.setAttribute('src', ("https:" == document.location.protocol ? "https://" : "http://") + "cdn.uservoice.com/javascripts/widgets/tab.js");
  document.getElementsByTagName('head')[0].appendChild(s);
}
_loadSuper = window.onload;
window.onload = (typeof window.onload != 'function') ? _loadUserVoice : function() { _loadSuper(); _loadUserVoice(); };
</script>
