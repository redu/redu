
<div id="all_content3">
<div id="col_content">
<div id="breadcrumbs"><a href="#">home</a> > <a href="#">aprender</a> > <a href="#">exames</a></div>
<div id="title">EXAMES 


<%  if logged_in? %>
    	<div id="task_bar_exames"><%= link_to 'Novo Exame', :action => 'new' %> |
			 <%= link_to 'Meus Exames', published_user_exams_path(current_user.id) %> |
			 <%= link_to 'Meus Favoritos', :action => 'show_favorites', :id => current_user %>
			 </div>
<% end %>

<%= render :partial => "exams/search" %> </div>


	
<!-- LISTAGEM RECURSO ############################################################################################################################################## -->

<div id="filter_bar">
    <span class="title_list">MEUS FAVORITOS</span>
    <span class="title_filter">Ordenar:</span>
		
 </div>
	  
    <div id="all_list">
    	
    	<% @favorites = current_user.get_favorites %>
		<% @favorites.each do |favorite| %>
		<% if favorite.favoritable_type == 'Exam'%>
		<% @hasExam = true %>
		<% end %>
		<% end %>
		<% if @favorites && @favorites.length > 0 && @hasExam %>
 			<%= render :partial => 'exams/favorite',:collection => @favorites, :as => :favorite  %> 
 		<% else %>
 			Não há favoritos ainda.
		<% end %>
		
    </div>
<!-- LISTAGEM RECURSO ############################################################################################################################################## -->
    
<!-- PAGINACAO ##################################################################################################################################################### -->
   
	
	<div id="pagination" align="center">
    	<ul id="pagination_number">
    		<li><a href="" class="nav">< Anterior</a></li>
            <li><a href="" class="number">1</a></li>
            <li><a href="" class="number">2</a></li>
            <li><a href="" class="number">3</a></li>
            <li><a href="" class="number">4</a></li>
            <li><a href="" class="number">5</a></li>
            <li><a href="" class="nav">Próximo ></a></li>
    	</ul>
    </div>
<!-- PAGINACAO ##################################################################################################################################################### -->
</div>
	
<!-- COLUNA DIREITA ################################################################################################################################################ -->
<div id="col_box3">
<!-- BOX MOVIMENTACAO RECENTE ///////////////////////////////////////////////////////////////////////////////////// -->
	<div id="col_box3">
	<% if current_user %>
	<% @logs = current_user.log_activity %>
	<%= render :partial => 'logs/log', :locals => {:logs => @logs} %>
	<% else %>
	<%= render :partial => 'logs/not_logged_in', :locals => {:logs => @logs} %>
	<% end %>
	</div>

</div>
<!--<div id="title_page">Assistindo Aula</div>-->
</div>
