<% if @exams && @exams.size > 0 %>
  <ul class="default">
    <%= render :partial => 'exams/item', :collection => @exams, :as => :exam %>
  </ul>
<% else %>
  (Nenhum exame)
<% end %>

<%= will_paginate @exams %>
