#page.select("#explanation_#{@question.id.to_s}").invoke('hide')

page << "document.getElementById('thebug_#{@question.id.to_s}').style.display = 'none';"

page.replace_html  'explanation_' + @question.id.to_s, :partial => 'exams/question_review', :locals => {:question => @question, :answer => @answer, :question_index => @index}  
          
page << "Effect.SlideDown('explanation_#{@question.id.to_s}');"
