<% if @has_prev %>
  <%= button_to_remote('Anterior', {:url => {:action => :answer, :id => session[:exam].id, :q_index => (session[:question_index] - 1)}, :loading => "showLoading();", :with => "addQuestionParams(#{@step.id})"}, :id => 'prev_answer_btn') %>
<% end %>

<% if @has_next %>
  <%= button_to_remote('Próximo', {:url => {:action => :answer, :id => session[:exam].id, :q_index => session[:question_index] + 1}, :loading => "showLoading();", :with => "addQuestionParams(#{@step.id})"}, :id => 'first_question_btn') %>
<% else %>
  <%= hidden_field_tag :question, @step.id %>
  <%= submit_tag "Finalizar", :id => 'submit_answer_btn' %>
<% end %>
