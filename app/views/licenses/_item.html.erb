<tr>
  <td><%= item.id %></td>
  <td class="user-name"><%= item.name %></td>
  <td class="user-login"><%= item.login %></td>
  <td><%= l item.period_start %></td>
  <td><%= item.period_end ? (l item.period_end) : "-" %></td>
  <td><%= Role.human_attribute_name(Role[item.role]) %></td>
</tr>
