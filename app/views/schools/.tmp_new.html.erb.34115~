<div id="breadcrumbs"><a href="<%=application_url%>">início</a> > <a href="#">redes</a> > <a href="#">nova rede</a> </div>
<h3 class="call">Nova Rede</h3>

	<% form_for @school, :html => {:id => "form_school"} do |f|%>
	<%= render "#{@school.current_step}_step", :f => f, :edit => false %>

<div class="grid_16">
	<%= f.submit "< Anterior", :name => "back_button" unless @school.first_step? %>
		<%= f.submit "Próximo >" unless @school.last_step? %> ou <%= link_to "Cancelar e voltar", cancel_schools_path%>
		</div>
	<% end %>

</div>
