<div class="grid_16">
  <%= render :partial => "spaces/sidebar", :locals => {:space => @space} %>

  <div class="main grid_12 omega">
    <div class="fake-tabs">
      <%= render :partial => 'spaces/taskbar' %>

      <div id="content">
        <% if can? :manage, @space %>
          <%= link_to_remote "Novo Módulo", :url => new_space_subject_path(@space),
            :method => :get,
            :loading => "$('#spinner-new-subject').show();",
            :success => "$('#spinner-new-subject').hide();",
            :html => { :id => 'link-new-subject' } %>
          <%= image_tag 'spinner.gif', :id => 'spinner-new-subject',
            :style => 'display: none;' %>
        <% end %>
        <%= render :partial => "subject_list"%>
        <%= will_paginate @subjects, :renderer => EndlessRenderer %>
      </div>
    </div>
  </div>
</div>
