<div class="alt first_alt">
  <%= link_to @forum.name, space_forum_path(@space) %>
  -
  <%= t :edit_topic %>
</div>

<%= render :partial => 'topics/form', :locals => {:space_id => @space.id, :topic => @topic, :form_new => false} %>
