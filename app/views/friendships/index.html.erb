<h1>Todos os amigos</h1>
<div id="friends_list">
  <div class="grid_16">
    <%= render :partial => "friendships/item", :collection => @friends, :as => :friend %>
  </div>
  <%= link_to "Voltar pro perfil", user_path(@user) %>
</div>
<%= will_paginate @friends, :renderer => EndlessRenderer %>
