var $form = $("#cadastro")
<% if @user.errors.empty? %>

<% else %>
  var $newForm = $("<%= escape_javascript render 'landing/modal_sign_up',
                   :user => @user %>");
  $form.replaceWith($newForm);
<% end %>
