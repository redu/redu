<%# Cabeçalho do environment (contém título e link para voltar )  %>

<div id="hierarchy-header" class="management clearfix">
  <h3 class="call name"><%= link_to h(environment.name), environment_path(environment) %></h3>
  <%= link_to "Sair", environment_path(environment),
    :class => "manage icon-manage-gray_8_10-before" %>
</div><!-- end #hierarchy-header -->
