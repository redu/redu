page.insert_html :bottom, 'bulletin_list',
  :partial => 'bulletins/new/item_admin', :collection => @bulletins
page.replace_html '.endless',
  (will_paginate @bulletins, :renderer => EndlessRenderer)

