page.insert_html :bottom, "#messages > tbody", :partial => 'messages/item',
  :collection => @messages,
  :locals => { :mailbox => :outbox }
page.replace_html ".endless",
  (will_paginate @messages, :renderer => EndlessRenderer)
