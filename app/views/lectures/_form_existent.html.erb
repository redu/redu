<div id="lecture_form">
  <%= form_for [@space, @subject, @lecture], :remote => true,
    :html => { :id => "existent-form", :class => "form-common" } do |f| %>
    <% field_set_tag do %>
    <%= f.label :lecture_id, "Recurso:" %>
      <% if user_existent_lectures.empty? %>
        <div class="box_notice">Você não possui nenhuma aula. Crie uma nova!</div>
      <% else %>
        <%= select_tag(:lecture_id, options_for_select(user_existent_lectures)) %>
        <%= f.submit 'Criar' %>
      <% end %>
      <%= hidden_field_tag "subject_id", @subject.id %>
      <%= link_to_function "Cancelar",
        "$('#existent-form').remove(); return false;",
        :class => 'cancel vanilla' %>
    <% end %>
  <% end %>
</div>
